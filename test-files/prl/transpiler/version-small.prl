header {
  prl_version 1.0
}

versioned feature Cnfl
versioned feature Inst

rule if Inst[ = 29] then Cnfl[ >= 1]
rule if Inst[ = 30] then Cnfl[ >= 1]
rule if Inst[ = 31] then Cnfl[ >= 1]
rule if Inst[ = 32] then Cnfl[ >= 1]
rule if Inst[ = 33] then Cnfl[ >= 1]
rule if Inst[ = 34] then Cnfl[ >= 1]
rule if Inst[ = 35] then Cnfl[ >= 1]
rule if Inst[ = 36] then Cnfl[ >= 1]
rule if Inst[ = 21] then Cnfl[ >= 1]
rule if Inst[ = 22] then Cnfl[ >= 1]
rule if Inst[ = 23] then Cnfl[ >= 1]
rule if Inst[ = 24] then Cnfl[ >= 1]
rule if Inst[ = 25] then Cnfl[ >= 1]
rule if Inst[ = 26] then Cnfl[ >= 1]
rule if Inst[ = 27] then Cnfl[ >= 1]
rule if Inst[ = 28] then Cnfl[ >= 1]
rule if Inst[ = 13] then Cnfl[ >= 1]
rule if Inst[ = 14] then Cnfl[ >= 1]
rule if Inst[ = 15] then Cnfl[ >= 1]
rule if Inst[ = 16] then Cnfl[ >= 1]
rule if Inst[ = 17] then Cnfl[ >= 1]
rule if Inst[ = 18] then Cnfl[ >= 1]
rule if Inst[ = 19] then Cnfl[ >= 1]
rule if Inst[ = 20] then Cnfl[ >= 1]
rule if Inst[ = 5] then Cnfl[ >= 1]
rule if Inst[ = 6] then Cnfl[ >= 1]
rule if Inst[ = 7] then Cnfl[ >= 1]
rule if Inst[ = 8] then Cnfl[ >= 1]
rule if Inst[ = 9] then Cnfl[ >= 1]
rule if Inst[ = 10] then Cnfl[ >= 1]
rule if Inst[ = 11] then Cnfl[ >= 1]
rule if Inst[ = 12] then Cnfl[ >= 1]
rule if Inst[ = 1] then Cnfl[ >= 1]
rule if Inst[ = 2] then Cnfl[ >= 1]
rule if Inst[ = 3] then Cnfl[ >= 1]
rule if Inst[ = 4] then Cnfl[ >= 1]
rule if Inst[ = 93] then Inst[ >= 1]
rule if Inst[ = 94] then Inst[ >= 1]
rule if Inst[ = 95] then Inst[ >= 1]
rule if Inst[ = 96] then Inst[ >= 1]
rule if Inst[ = 97] then Inst[ >= 1]
rule if Inst[ = 98] then Inst[ >= 1]
rule if Inst[ = 99] then Inst[ >= 1]
rule if Inst[ = 100] then Inst[ >= 1]
rule if Inst[ = 85] then Inst[ >= 1]
rule if Inst[ = 86] then Inst[ >= 1]
rule if Inst[ = 87] then Inst[ >= 1]
rule if Inst[ = 88] then Inst[ >= 1]
rule if Inst[ = 89] then Inst[ >= 1]
rule if Inst[ = 90] then Inst[ >= 1]
rule if Inst[ = 91] then Inst[ >= 1]
rule if Inst[ = 92] then Inst[ >= 1]
rule if Inst[ = 77] then Cnfl[ >= 1]
rule if Inst[ = 78] then Cnfl[ >= 1]
rule if Inst[ = 79] then Cnfl[ >= 1]
rule if Inst[ = 80] then Inst[ >= 1]
rule if Inst[ = 81] then Inst[ >= 1]
rule if Inst[ = 82] then Inst[ >= 1]
rule if Inst[ = 83] then Inst[ >= 1]
rule if Inst[ = 84] then Inst[ >= 1]
rule if Inst[ = 69] then Cnfl[ >= 1]
rule if Inst[ = 70] then Cnfl[ >= 1]
rule if Inst[ = 71] then Cnfl[ >= 1]
rule if Inst[ = 72] then Cnfl[ >= 1]
rule if Inst[ = 73] then Cnfl[ >= 1]
rule if Inst[ = 74] then Cnfl[ >= 1]
rule if Inst[ = 75] then Cnfl[ >= 1]
rule if Inst[ = 76] then Cnfl[ >= 1]
rule if Inst[ = 61] then Cnfl[ >= 1]
rule if Inst[ = 62] then Cnfl[ >= 1]
rule if Inst[ = 63] then Cnfl[ >= 1]
rule if Inst[ = 64] then Cnfl[ >= 1]
rule if Inst[ = 65] then Cnfl[ >= 1]
rule if Inst[ = 66] then Cnfl[ >= 1]
rule if Inst[ = 67] then Cnfl[ >= 1]
rule if Inst[ = 68] then Cnfl[ >= 1]
rule if Inst[ = 53] then Cnfl[ >= 1]
rule if Inst[ = 54] then Cnfl[ >= 1]
rule if Inst[ = 55] then Cnfl[ >= 1]
rule if Inst[ = 56] then Cnfl[ >= 1]
rule if Inst[ = 57] then Cnfl[ >= 1]
rule if Inst[ = 58] then Cnfl[ >= 1]
rule if Inst[ = 59] then Cnfl[ >= 1]
rule if Inst[ = 60] then Cnfl[ >= 1]
rule if Inst[ = 45] then Cnfl[ >= 1]
rule if Inst[ = 46] then Cnfl[ >= 1]
rule if Inst[ = 47] then Cnfl[ >= 1]
rule if Inst[ = 48] then Cnfl[ >= 1]
rule if Inst[ = 49] then Cnfl[ >= 1]
rule if Inst[ = 50] then Cnfl[ >= 1]
rule if Inst[ = 51] then Cnfl[ >= 1]
rule if Inst[ = 52] then Cnfl[ >= 1]
rule if Inst[ = 37] then Cnfl[ >= 1]
rule if Inst[ = 38] then Cnfl[ >= 1]
rule if Inst[ = 39] then Cnfl[ >= 1]
rule if Inst[ = 40] then Cnfl[ >= 1]
rule if Inst[ = 41] then Cnfl[ >= 1]
rule if Inst[ = 42] then Cnfl[ >= 1]
rule if Inst[ = 43] then Cnfl[ >= 1]
rule if Inst[ = 44] then Cnfl[ >= 1]

